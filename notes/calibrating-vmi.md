# My guide to VMI Calibration
This is a blog post.

* Step 1: Pump down the chamber.
* Step 2: Turn on the laser.
4. **Warning regarding local testing:** If you simply double-click `index.html` to open it in Chrome from your desktop (using the `file:///` protocol), the browser's security settings will block it from loading the `.md` file. 
* To test it on your computer, you need to use an extension like **"Live Server"** in VS Code. 
* Alternatively, just commit the `index.html` and your new `notes` folder, push it to GitHub, and it will work perfectly on your live `tsendsuren.github.io` link!